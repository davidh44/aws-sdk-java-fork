version: 0.2

phases:

  install:
    runtime-versions:
      java: "corretto8"

  pre_build:
    commands:
    - export CODEARTIFACT_AUTH_TOKEN=`aws codeartifact get-authorization-token --domain amazon --domain-owner 149122183214 --region us-west-2 --query authorizationToken --output text`

  build:
    commands:
    - mvn -s release-resources/buildspecs/resources/release-settings.xml clean package -Ppublishing,staging -Dfindbugs.skip -DskipTests -Dcheckstyle.skip -DadditionalJOption=-Xdoclint:none -DadditionalJOption=--allow-script-in-comments
artifacts:
  files:
    - aws-java-sdk-osgi/target/aws-java-sdk.zip
    - pom.xml
    - release-resources/buildspecs/*.yml
